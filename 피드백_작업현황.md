# 피드백 작업 현황 브리핑
> 작성일: 2026-02-27

---

## ✅ 완료된 작업

| 파일 | 변경 내용 |
|------|-----------|
| `index.html` | 앱 이름 "Lovable App" → "Abstract Echoes", 파비콘 추가, Lovable 참조 전부 제거 |
| `src/lib/tuningParams.ts` | 디폴트 파라미터 전부 사용자 제공 JSON 값으로 교체 (loveRing 3→8, thanksEmber 15→28, missyouStar 40→75 등) |
| `src/lib/yamnetClassifier.ts` | **박수/스냅/웃음 인식 버그 수정** — AudioClassifier 생성 시 threshold가 고정되어 이후 튜닝이 무효인 버그. scoreThreshold=0으로 생성 후 classify()에서 클라이언트 사이드 필터링으로 전환 |
| `src/lib/generativeEngine.ts` | • IDLE 모드 구조 추가 (`setIdleMode()`, `idleAnimTick()`) <br>• 링 시각 변형: 점선/다각형/웨이브 3종 랜덤 적용 (동심원 단조로움 해결) <br>• 고마워 불씨 지속시간 증가 (`life: 1.5+r` → `life: 2.5+r*1.5`) <br>• IDLE 화면 환경 애니메이션 개선 (성운 블룸, 파티클 부유) |

---

## 📋 남은 작업

### 소규모 (각각 10분 이내)

| # | 파일 | 작업 |
|---|------|------|
| C | `SoundCanvas.tsx` | **해상도 1408×960 → 1720×1032** (LED wall 실제 해상도) |
| D | `SoundCanvas.tsx` | 메인 UI 제목/설명문 개선 ("SOUND REACTIVE" → 새 디자인) |
| E | `SoundCanvas.tsx` | 음성 인식 텍스트 위치: 정중앙 → 상단 중앙, 스타일 개선 |
| G | `SoundCanvas.tsx` | QR 카운트다운 종료 시 → 캔버스 초기화 + IDLE 상태 복귀 |
| H | `전시_시작.bat` | 키오스크 제목표시줄 감추기 (`--kiosk --app=` 플래그 충돌 수정) |
| A | `generativeEngine.ts` | 행복해: 테두리 라인 1프레임 즉시 등장 → 순차적 등장 (scheduleEffect 딜레이) |
| B | `generativeEngine.ts` | 세로 저장 이미지: center-crop → letterbox (전체 캔버스 담기) |

### 중규모 (설계 필요)

| # | 파일 | 작업 | 접근법 |
|---|------|------|--------|
| F | `SoundCanvas.tsx` | 목소리 감지 시 자동 그리기 시작 | 마이크 ON 상태에서 `isIdleMode` 상태 추가. 앱 시작 → IDLE 화면(제목+애니) → 목소리 감지 → 그리기 모드로 전환. QR 종료 시 다시 IDLE 복귀 |

---

## ❌ 미결 / 판단 필요

| 항목 | 이슈 |
|------|------|
| **기본 IDLE 애니메이션** | `generativeEngine.ts`에 `idleAnimTick()` 구조는 추가됨. 실제 IDLE 상태 진입 로직(`isIdleMode`)은 F번 작업과 연계 필요 |
| **키오스크 모드 제목표시줄** | 현재 .bat이 `--kiosk --app=URL` 두 플래그를 동시 사용 중. `--app` 제거하고 `--kiosk URL`만 사용하면 해결될 가능성 높음 |

---

## 권장 진행 순서

```
1단계 (빠른 수정): C → D → E → H → A → B → G
2단계 (구조 변경): F (IDLE 상태 머신)
```

> **주의**: F번 작업은 SoundCanvas.tsx 전반을 수정하므로 나머지 완료 후 마지막에 작업 권장.
